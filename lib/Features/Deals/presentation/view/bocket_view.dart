import 'package:flutter/material.dart';

class BocketView extends StatelessWidget {
  const BocketView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
